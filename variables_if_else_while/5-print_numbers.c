#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 *
 * description - 'numbers'
 *
 * Return: always success 0
 *
 */
int main(void)
{
int digit;
for (digit = 1; digit < 10; digit++)
{
printf("%d\n",digit);
}
return (0);
}
