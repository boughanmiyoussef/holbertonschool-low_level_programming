#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Description: 'numbers '
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int digit;
for (digit = 1; digit < 10; digit++)
{
printf("%d", digit);
}
printf("\n");
return (0);
}
