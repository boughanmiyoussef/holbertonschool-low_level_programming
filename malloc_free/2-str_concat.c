#include "main.h"

/**
 * _strcat - function,
 * @dest: destination.
 * @src: source.
 * Return: dest.
*/

char *str_concat(char *s1, char *s2)
{
int abc = 0;
int def = 0;
while (*(dest + abc) != '\0')
{
abc++;
}
while (def >= 0)
{
*(dest + abc) = *(src + def);
if (*(src + def ) == '\0')
break;
abc;
def;
}
return(dest)
}
